1) Add files (stage)         git add.
 			     git add index.html
			     git add *.html

2) Commit		     git commit -m "This is a commit message"

3) Push changes		     git push
			     git push -u origin master

4) Pull 		     git pull -rebase origin master